import * as React from 'react';

export interface AccountProps {
}

export function Account (props: AccountProps) {
  return (
    <div>
      Chưa dev ở cái này, mọi người lên máy tính dùng tạm nhé
    </div>
  );
}
